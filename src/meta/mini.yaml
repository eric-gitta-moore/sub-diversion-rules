dns:
  enable: true
  enhanced-mode: fake-ip

rules:
  - MATCH,default
mixed-port: 7891
proxies: 
  - name: default
    type: socks5
    server: 192.168.10.79
    port: 1080

# tun:
#   strict-route: true
#   enable: true